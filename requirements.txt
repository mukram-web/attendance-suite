streamlit>=1.49
openpyxl>=3.1
pandas>=2.1
altair>=5.0
# Live Google Drive mode (needed by the deployed auto app; imported lazily,
# so manual-upload use works without these installed locally):
google-api-python-client>=2.0
google-auth>=2.0
# Dashboard live reads (gspread resolves COUNTIF formula cells to Present/Absent
# via FORMATTED_VALUE) + Plotly charts:
gspread>=6.0
plotly>=5.18
# Prebuilt dashboard store (written by pipeline.py, read by the app):
duckdb>=1.0
# The dashboard's "Add data" page: puts the week's Zoom exports on Drive and
# asks GitHub Actions to run the pipeline. Streamlit already pulls requests
# in, but this page depends on it directly, so it is declared.
requests>=2.31
